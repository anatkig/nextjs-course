import type { Module } from '../types';
export const module13: Module = { id: 'mod-13', title: 'Testing Next.js Apps', description: 'Unit testing, integration testing, and E2E testing with Jest, React Testing Library, and Playwright. Write tests for Server Components, Client Components, Route Handlers, and full user flows to ensure reliability across your entire Next.js application.',
  topics: [
    { id: 'mod13-t1', title: 'Unit & Integration Testing', explanation: `## Testing Setup\n\nNext.js apps can be tested at multiple levels: unit tests for individual functions and components, integration tests for combined behavior, and E2E tests for full user flows. The standard stack uses Jest and React Testing Library.\n\n### Jest + React Testing Library\n\n\`\`\`bash\nnpm install -D jest @testing-library/react @testing-library/jest-dom jest-environment-jsdom\n\`\`\`\n\n### Testing Server Components\n\nServer Components are async functions, so you need to \`await\` them to get the JSX output before passing it to \`render()\`. Since tests run in a Node/jsdom environment (not a browser), you must also mock \`fetch\` calls:\n\n\`\`\`tsx\n// __tests__/page.test.tsx\nimport { render, screen } from '@testing-library/react';\nimport Page from '../app/page';\n\n// Mock fetch for Server Components \u2014 required since tests run in Node, not a browser\nglobal.fetch = jest.fn(() =>\n  Promise.resolve({\n    ok: true,\n    json: () => Promise.resolve([{ id: 1, title: 'Test Post' }]),\n  })\n) as jest.Mock;\n\ndescribe('Home Page', () => {\n  it('renders posts', async () => {\n    const page = await Page();\n    render(page);\n    expect(screen.getByText('Test Post')).toBeInTheDocument();\n  });\n});\n\`\`\`\n\n### Testing Client Components\n\nClient Components are tested like traditional React components \u2014 render them and assert on the DOM output. Use \`fireEvent\` or \`userEvent\` to simulate user interactions:\n\n\`\`\`tsx\nimport { render, screen, fireEvent } from '@testing-library/react';\nimport { Counter } from '../components/Counter';\n\ndescribe('Counter', () => {\n  it('increments on click', () => {\n    render(<Counter />);\n    const button = screen.getByRole('button');\n\n    expect(button).toHaveTextContent('Count: 0');\n    fireEvent.click(button);\n    expect(button).toHaveTextContent('Count: 1');\n  });\n});\n\`\`\`\n\n### Testing Server Actions\n\nServer Actions are just async functions, so you can import and call them directly in tests. Mock Next.js-specific functions like \`revalidatePath\` since they depend on the request context:\n\n\`\`\`tsx\nimport { createPost } from '../app/actions';\n\njest.mock('next/cache', () => ({\n  revalidatePath: jest.fn(),\n}));\n\ndescribe('createPost', () => {\n  it('creates a post and revalidates', async () => {\n    const formData = new FormData();\n    formData.set('title', 'Test');\n    formData.set('content', 'Content');\n\n    const result = await createPost(formData);\n    expect(result.success).toBe(true);\n  });\n});\n\`\`\`\n\n### Testing Route Handlers\n\nRoute Handlers export standard async functions that accept Request objects and return Response objects. This makes them straightforward to test without any special framework setup:\n\n\`\`\`tsx\nimport { GET, POST } from '../app/api/users/route';\n\ndescribe('/api/users', () => {\n  it('GET returns users', async () => {\n    const response = await GET();\n    const data = await response.json();\n    expect(data).toHaveLength(2);\n  });\n\n  it('POST creates a user', async () => {\n    const request = new Request('http://localhost/api/users', {\n      method: 'POST',\n      body: JSON.stringify({ name: 'Test', email: 'test@test.com' }),\n    });\n    const response = await POST(request);\n    expect(response.status).toBe(201);\n  });\n});\n\`\`\``, tasks: [{ description: 'Write tests for a Counter Client Component (with click handling) and a Route Handler (GET endpoint that returns a list of items).', starterCode: `// __tests__/Counter.test.tsx\n// TODO: Test that Counter renders and increments\n\n// __tests__/api.test.ts\n// TODO: Test GET route handler`, solution: `// __tests__/Counter.test.tsx\nimport { render, screen, fireEvent } from '@testing-library/react';\nimport '@testing-library/jest-dom';\n\n// Assume Counter component\nfunction Counter() {\n  const [count, setCount] = require('react').useState(0);\n  return <button onClick={() => setCount(c => c + 1)}>Count: {count}</button>;\n}\n\ndescribe('Counter', () => {\n  it('renders initial count of 0', () => {\n    render(<Counter />);\n    expect(screen.getByRole('button')).toHaveTextContent('Count: 0');\n  });\n\n  it('increments count on click', () => {\n    render(<Counter />);\n    const button = screen.getByRole('button');\n    fireEvent.click(button);\n    expect(button).toHaveTextContent('Count: 1');\n    fireEvent.click(button);\n    expect(button).toHaveTextContent('Count: 2');\n  });\n});\n\n// __tests__/api.test.ts\ndescribe('/api/items', () => {\n  it('GET returns a list of items', async () => {\n    const response = await fetch('http://localhost:3000/api/items');\n    const data = await response.json();\n    expect(Array.isArray(data)).toBe(true);\n    expect(response.status).toBe(200);\n  });\n});`, hints: ['Import @testing-library/jest-dom for custom matchers like toHaveTextContent', 'Use fireEvent.click to simulate user clicks', 'Test Route Handlers by calling the exported function directly'] }] },
    { id: 'mod13-t2', title: 'E2E Testing with Playwright', explanation: `## Playwright for E2E Testing\n\nEnd-to-end tests verify complete user flows in a real browser environment. Playwright is the recommended E2E framework for Next.js \u2014 it supports all modern browsers, auto-waits for elements, and can start your dev server automatically.\n\n\`\`\`bash\nnpm install -D @playwright/test\nnpx playwright install\n\`\`\`\n\n### Configuration\n\n\`\`\`ts\n// playwright.config.ts\nimport { defineConfig } from '@playwright/test';\n\nexport default defineConfig({\n  testDir: './e2e',\n  // Automatically start the dev server before running E2E tests\n  webServer: {\n    command: 'npm run dev',\n    port: 3000,\n    reuseExistingServer: !process.env.CI, // Skip startup if server already running locally\n  },\n  use: {\n    baseURL: 'http://localhost:3000',\n  },\n});\n\`\`\`\n\n### Writing E2E Tests\n\nPlaywright provides an intuitive API for browser automation. Tests use \`page.goto()\` for navigation, \`page.click()\` and \`page.fill()\` for interactions, and \`expect()\` for assertions. Playwright auto-waits for elements to be ready before interacting:\n\n\`\`\`ts\nimport { test, expect } from '@playwright/test';\n\ntest('homepage loads and navigation works', async ({ page }) => {\n  await page.goto('/');\n  await expect(page.locator('h1')).toContainText('Welcome');\n\n  await page.click('text=About');\n  await expect(page).toHaveURL('/about');\n  await expect(page.locator('h1')).toContainText('About');\n});\n\ntest('form submission works', async ({ page }) => {\n  await page.goto('/contact');\n\n  await page.fill('input[name="email"]', 'test@example.com');\n  await page.fill('textarea[name="message"]', 'Hello!');\n  await page.click('button[type="submit"]');\n\n  await expect(page.locator('.success')).toBeVisible();\n});\n\ntest('authentication flow', async ({ page }) => {\n  await page.goto('/dashboard');\n  // Should redirect to login\n  await expect(page).toHaveURL('/login');\n\n  await page.fill('input[name="email"]', 'user@test.com');\n  await page.fill('input[name="password"]', 'password');\n  await page.click('button[type="submit"]');\n\n  // Should redirect to dashboard\n  await expect(page).toHaveURL('/dashboard');\n  await expect(page.locator('h1')).toContainText('Dashboard');\n});\n\`\`\``, tasks: [{ description: 'Write a Playwright E2E test that navigates to the homepage, verifies the heading, clicks a navigation link, and verifies the new page content.', starterCode: `// e2e/navigation.spec.ts\nimport { test, expect } from '@playwright/test';\n\n// TODO: Write navigation E2E test`, solution: `// e2e/navigation.spec.ts\nimport { test, expect } from '@playwright/test';\n\ntest.describe('Navigation', () => {\n  test('can navigate from home to about page', async ({ page }) => {\n    await page.goto('/');\n    await expect(page.locator('h1')).toContainText('Welcome');\n\n    await page.click('a[href="/about"]');\n    await expect(page).toHaveURL('/about');\n    await expect(page.locator('h1')).toContainText('About');\n  });\n\n  test('can navigate back to home', async ({ page }) => {\n    await page.goto('/about');\n    await page.click('a[href="/"]');\n    await expect(page).toHaveURL('/');\n  });\n\n  test('404 page shows for unknown routes', async ({ page }) => {\n    const response = await page.goto('/unknown-route');\n    expect(response?.status()).toBe(404);\n  });\n});`, hints: ['Use page.goto() to navigate to URLs', 'Use expect(page.locator(...)) for assertions', 'Use page.click() and page.fill() for interactions'] }] },
  ],
  test: [
    { id: 'mod13-q1', question: 'How do you test an async Server Component?', options: ['render(<Component />)', 'const result = await Component(); render(result)', 'mount(<Component />)', 'shallow(<Component />)'], correctAnswer: 1, explanation: 'Server Components are async functions. You need to await them first, then render the result in the test.' },
    { id: 'mod13-q2', question: 'What library provides toHaveTextContent and toBeInTheDocument matchers?', options: ['jest', '@testing-library/react', '@testing-library/jest-dom', 'playwright'], correctAnswer: 2, explanation: '@testing-library/jest-dom provides custom DOM matchers like toHaveTextContent, toBeInTheDocument, toBeVisible, etc.' },
    { id: 'mod13-q3', question: 'What does Playwright\'s webServer config option do?', options: ['Mocks the server', 'Starts the dev server before running tests', 'Creates a test database', 'Deploys the app'], correctAnswer: 1, explanation: 'The webServer option automatically starts the Next.js dev server before running tests and shuts it down afterwards.' },
    { id: 'mod13-q4', question: 'How do you test a Server Action?', options: ['Import and call it directly with mock data', 'Use Playwright', 'Use Cypress', 'You cannot test Server Actions'], correctAnswer: 0, explanation: 'Server Actions can be tested directly by importing and calling them with FormData, while mocking dependencies like revalidatePath.' },
    { id: 'mod13-q5', question: 'What is the difference between unit tests and E2E tests?', options: ['No difference', 'Unit tests test isolated functions; E2E tests test the full app flow in a browser', 'Unit tests are slower', 'E2E tests only test APIs'], correctAnswer: 1, explanation: 'Unit tests verify individual functions/components in isolation, while E2E tests verify complete user flows in a real browser environment.' },
  ],
};
